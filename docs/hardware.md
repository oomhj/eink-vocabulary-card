# 硬件说明 — 2.13" 墨水屏单词卡（V0.1）

依据 `Schematic_213墨水屏单词卡.svg`（EasyEDA 原理图，Sheet "2.13墨水屏单词卡 copy"）整理。
原理图为 SVG 文本，可用文本方式搜索元件位号与网络标号。

> ⚠️ **固件定位**：本原理图主控为 **ESP8266（ESP-12S）**，`src/word_cards_ref.cpp` 是 **ESP32 参考代码**（移植自 TTGO T5），未适配本硬件、**不可直接编译**。它只用于描述行为（索引建词库→轻睡→随机刷词→按键唤醒），真正要写的是面向本原理图的 ESP8266 固件。引脚/API 移植对照见文末 [固件移植指引](#12-固件移植指引)。

## 1. 板卡概览

| 项 | 内容 |
|---|---|
| 板卡 | 2.13墨水屏单词卡 V0.1 |
| 主控 | ESP-12S 模组（ESP8266，4MB flash，`huge_app` 分区） |
| 显示屏 | 2.13" SSD1680，HINK-E0266A15-A0，FPC 0.5mm 24P 翻盖座（P1 插座 / MSP1 屏符号） |
| USB | USB Type-C（仅充电）+ 6P 烧录焊盘（无板上 CH340） |
| 供电 | USB 5V 或锂电池；TP4054 充电 + RT9013-33GB LDO 3.3V |
| 存储 | 无 TF（词典在 flash/PROGMEM） |
| 按键 | 上键（唯一功能键）+ 唤醒键（RST） |

## 2. 系统框图

```
                 USB-C (U262-16XN-4BVC11)
                /         |        \
        USB_D+/D-       VBUS      (充电)
          |               |         |
        CH340C (U1)     D2 MBR0530  |
          |              VIN+       |
    TXD/RXD -> ESP8266   |          |
    DTR/RTS -> 自动下载  |   TP4054 (U8) 充电
          |    (Q2/Q7)   |     | (R30 设电流)
          |              |   BAT+ ── CN2 电池座(ZH1.5-2P)
          |              |     |
          |        RT9013-33GB (U3) LDO
          |              |
          |           VCC_3V3 (3.3V)
          |              |
      +---+---+----+-----+--------+-------+----+
      |   |   |    |     |        |       |    |
   ESP-12S (U2)  P1  上键  唤醒  LED×2  电池检测
    (ESP8266)   显示屏  RST             (→ADC)
```

## 3. 核心器件（BOM）

| 位号 | 器件 | 型号/值 | 说明 |
|---|---|---|---|
| U2 | ESP-12S | ESP8266 模组 | 主控（esp12e/esp12f 兼容） |
| J1 | 烧录焊盘 6P | TXD/RXD/GPIO0/RST/GND/3V3 | 外接 USB-TTL 刷固件（无板上 CH340） |
| U3 | RT9013-33GB | SOT-23-5 (LCSC C47773) | 3.3V LDO，系统电源 |
| U8 | TP4054 | SOT-23-5 (LCSC C668215) | 锂电池线性充电 |
| TYPEC16P | USB-C 座 | U262-16XN-4BVC11 (LCSC C319148) | **仅充电**（VBUS→D2→TP4054），D+/D- 悬空 |
| P1 | 显示屏 FPC | 0.5mm 24P 翻盖座 H2.0 (LCSC C11092) | 屏插座（实际贴装）；MSP1 为屏符号（HINK 面板） |
| CN2 | 电池座 | ZH1.5-2P-WT (LCSC C722913) | 锂电池接口 |
| S1 | 拨动开关 | MSK-12C01-07 | RT9013 的 EN 使能开关（整机电源开关），与 R18 二选一 |
| SW2/SW4 | 轻触按键 | TS-1010-C-A / 4×4×1.5 防水 | 上键 / 唤醒 |
| D2 | 肖特基 | MBR0530 (LCSC C77336) | VBUS 防倒灌 |
| Q9 | N-MOS | SI2302 | 墨水屏升压开关（与 L1 68µH 组成升压，供 VGH/VGL） |
| Q6 | N-MOS | SI2302 | 电池采样低边开关（IO12 控制，见 §10） |
| Q8 | P-MOS | SI2305 | 充电切换（插 USB 断开电池→LDO，R10/C19 配合，见 §4） |
| L1 | 电感 | 68µH (LCSC C142404) | 显示供电升压（VGH/VGL 相关） |
| LED1 | 红 LED | LED-0603 | 状态灯（串 R29） |
| LED0603_RED | 红 LED | LED0603_RED | TP4054 充电指示灯 |
| R30 | 电阻 | 10k/2k/1.66k | 充电电流配置（见 §4） |
| R18 | 0Ω | R0603 | RT9013 EN 常通（焊上则 LDO 常开），与 S1 二选一 |

> **贴片封装（手工焊接）**：电阻全部 **0603**；电容除 C2=0805 外均 **0603**。
> **C3/C4/C5（1µF 屏电源去耦）已由 0402 改为 0603**——整板无 0402，全手工友好。
> 需热风焊接的：ESP-12S 模组、USB-C 座、FPC 座；其余尖头烙铁可焊。

## 4. 电源架构

```
USB-C VBUS ──D2(MBR0530)── VIN+ (5V)
                              ├─→ TP4054 (U8) 充电 ─→ BAT+ ── CN2 电池座
                              └─→ RT9013-33GB (U3) VIN
BAT+ ──Q8(SI2305 P-MOS, R10 栅极)──→ RT9013-33GB (U3) VIN   [插USB断开]
RT9013 VOUT = VCC_3V3 (3.3V 系统电源)
RT9013 EN ── (S1 拨动开关 或 R18 0Ω，二选一) ── 整机电源通断
```

- **充电**：TP4054（U8）线性充电，`CHRG` 脚驱动充电指示灯 `LED0603_RED`。
  充电电流由 **R30** 配置：10k=100mA、2k=400mA、1.66k≈500mA（原理图注释默认配 800mA 电池）。
- **充电切换（Q8/R10/C19）**：Q8(SI2305 P-MOS) 为电池→LDO 高边开关，R10(1k) 栅极驱动，C19 去耦。
  插 USB 时 VIN+ 供 LDO、**Q8 断开电池通路**（电池只充电不带载）；拔 USB 后 Q8 导通，电池供 LDO。
- **3.3V**：RT9013-33GB（U3，带使能脚的低压差 3.3V LDO）输出 `VCC_3V3`，为模组/屏/卡供电。
- **R18 与 S1（RT9013 的 EN 脚）**：原理图标注"R18 和开关 S1 二选一"——焊 **R18(0Ω)** 则 LDO 常使能（常开）；
  装 **S1（MSK-12C01-07 拨动开关）** 则通过 EN 脚手动控制整机 3.3V 通断，即**整机电源开关**。
- 板级注释：LDO 最低工作电压按 3.5V 计算电池 0%（低于此设备断电）——见固件 `battCurve[]`。

## 5. ESP-12S 模组（U2）引脚分配

ESP-12S 共 16 脚，左 8（下→上：1~8）/ 右 8（下→上：16~9）：

| 脚 | 引脚 | 网络/去向 | 脚 | 引脚 | 网络/去向 |
|---|---|---|---|---|---|
| 1 | RST | 唤醒键 SW4 + R33(10k 上拉) | 9 | GND | 地 |
| 2 | ADC | 电池电压检测输出 | 10 | IO15 | 显示屏 CS |
| 3 | EN(CH_PD) | 使能 | 11 | IO2 | 显示屏 RST |
| 4 | GPIO16 | **深睡唤醒**（独立网络，不直连 RST） | 12 | IO0 | **上键 SW2**（经 R5 100Ω 接地，strap 烧录） |
| 5 | IO14 | 显示屏 SCK | 13 | IO4 | 显示屏 BUSY |
| 6 | IO12 | 电池采样使能（HIGH 接通分压） | 14 | IO5 | **显示屏 DC** |
| 7 | IO13 | 显示屏 SDA | 15 | RXD0 | 烧录焊盘（外接 USB-TTL TX） |
| 8 | VCC | 3.3V | 16 | TXD0 | 烧录焊盘（外接 USB-TTL RX） |

> IO0/IO2/IO15 为启动 strap 引脚（IO0、IO2 需上电为高，IO15 需为低）；**IO15 在 BOOT 块有 10k 下拉到 GND**，保证上电 IO15 为低（boot mode (3,6)，SPI flash 启动正确 strap）。IO0 只接按键（上电按住=下载模式），IO2/IO15 接显示屏，上电时序需保证屏接口不干扰 strap 电平。（IO7 上拉修复后 boot mode 稳定为 **(3,7)**，两种均为 SPI flash boot，正常；历史日志里 (3,7) 曾为 RST 释放瞬态采样值。）

> ✅ **唤醒/复位电路（BOOT 块，2026-08 按 v2 原理图重新核对）**：SW4（唤醒键）经 R33(10k)+D4(MBR0530，阳极朝 RST 侧) 把 RST 网络拉低；RST 网络另有 C18(10nF)+10k 到 GND（隔直滤波，无直流负载）。原理图上 IO15/IO16/RST 三个 net flag 仅视觉相邻，**实为三个独立网络**。**烧录无需断开任何器件**：SW4 断开时 D4 支路开路，USB-TTL 可直接从 RST 焊盘干净复位进下载模式（2026-08 实测：烧录 + 定时深睡唤醒均正常，按键/二极管全程连接）。

> ⚠️ **RST 二次按压 / 深睡定时唤醒挂死 —— 已硬件修复（2026-08）**：现象为 RST 唤醒“一次失败一次成功”（`ets` 后无 `load`）、深睡 RTC 定时唤醒永久挂死。根因：ESP8266 **IO7（SPIQ，flash 数据线）悬空**，RST 释放瞬间电平毛刺使 boot ROM 读 flash 偶发失败。**修复：IO7 焊盘 10k 上拉 3.3V**（ESP-12S 未引出 IO7，需从裸片/模块对应焊盘接线）。修复后 RST 单按即醒、深睡定时唤醒每次干净（`rst cause:2 → load` 完整 boot，多轮实测稳定）。`error: pll_cal exceeds 2ms!!!` 每 boot 必现但纯 cosmetic（不阻塞启动），待后续处理。附注：flash 提速 40MHz（`board_build.f_flash`）亦可重试，未验证；FPM light sleep 为固件死路（RF 关不睡 / RF 开 `wifi_fpm_do_sleep` 空指针崩溃），勿再尝试。

> ✅ **无引脚复用**：下键已取消，IO13 纯屏 SDA。全板引脚全部专用，固件无需分时复用。

## 6. 显示屏连接（P1，FPC 0.5mm 24P）

> 物理 24P 布局 = **GDEY0213B74 布局**（VGH/VGL 在 21/23、4 脚 NC、5 脚 VSH2）。4 线 SPI，横屏 250×122。

| 屏脚 | 信号 | 网络 | → MCU/去向 |
|---|---|---|---|
| 24 | VCOM | — | 电荷泵电容 |
| 23 | VGL | — | **L1(68µH)+Q9(SI2302) 升压**（预电荷泵节点） |
| 22 | VSL | — | 电荷泵电容 |
| 21 | VGH | — | **L1(68µH)+Q9(SI2302) 升压**（预电荷泵节点） |
| 20 | VSH1 | — | 电荷泵电容 |
| 19 | VPP | — | 仅供测试 |
| 18 | VDD | — | C3 1µF 去耦（0603） |
| 17 | GND | — | 地 |
| 16 | VCC(VCI) | — | VCC_3V3 |
| 15 | VCC(VDDIO) | — | VCC_3V3（C1 0.1µF 去耦） |
| 14 | SDA | EPD_SDA | **IO13**（SPI MOSI） |
| 13 | SCK | EPD_SCK | **IO14**（SPI SCLK） |
| 12 | CS | CS | **IO15** |
| 11 | DC | DC | **IO5** |
| 10 | RST | DISPLAY_RST | **IO2** |
| 9 | BUSY | BUSY | **IO4** |
| 8 | BS | — | 电平选择（SPI 模式） |
| 7 | TSDA | — | 温度传感器数据 |
| 6 | TSCL | — | 温度传感器时钟 |
| 5 | VSH2 | — | **电容接地** |
| 4 | NC | — | 不接 |
| 3 | RESE | — | — |
| 2 | GDR | — | — |
| 1 | NC | — | — |

- **P1 = FPC 24P 插座（C11092，实际贴装）；MSP1 = 显示屏符号**（HINK-E0266A15-A0 面板 24P，示意用），两符号同脚位。
- SPI 总线即 ESP8266 硬件 SPI：SCLK=IO14，MOSI=IO13。
- 屏供电：VCC(15/16)≈VDDIO/VCI 接 VCC_3V3，GND(17)=VSS，VDD(18)=核心逻辑电源（C3 1µF 去耦，0603）。
- **VGH/VGL（21/23）由 L1（68µH）+ Q9（SI2302）升压供电**（预电荷泵节点，最终电压由 IC 内部电荷泵产生；Q1 SI1304BDL 已移除）；**4 脚 NC、5 脚 VSH2 电容接地**。
- 连接器说明标注："FPC 0.5MM 24P Pull type H2.0mm"。

### 与 GDEY0213B74 兼容性

物理 24P 引脚布局**完全一致，可互换**（含 4 脚 NC、5 脚 VSH2、21/23 VGH/VGL）。
差异仅在驱动 IC 与供电方式：GDEY0213B74 = **SSD1680Z**（内部电荷泵）；本屏原理图标注 SSD1680，但按 HINK-E02xxx 家族（T5 实物 HINK-E0213A22-A0 为 SSD1675B）+ 外部 VGH/VGL 升压推断**实为 SSD1675B**——详见 [`ink_displays.md`](ink_displays.md) §6。
固件 GxEPD2 类：SSD1675B → `GxEPD2_213_B73`（参考固件所用）；SSD1680 → `GxEPD2_213_B74`。建议按实物驱动确认。

> ⚠️ **实物屏实测（2026-08）**：已购 HINK-E0213A04-G01 的控制器**确认为 IL3895**（GDE0213B1 类，GxEPD2 类 `GxEPD2_213`），并非上文推断的 SSD1675B。该屏 B73/B74 驱动均不显示，仅 `GxEPD2_213`（IL3895）全刷时序正常。VCOM（0x2C）经调参定为 **0x18**（库默认 0xa8），落地为自定义类 `GxEPD2_213_HINK`（`src/hink_e0213.*`）。详见 [`ink_displays.md`](ink_displays.md) §10。选型/换料务必以实物驱动 IC 为准。

> ✅ **三色屏实测（2026-08）**：E0213A192 / HINK-E0213A127（SSD1680 三色，`GxEPD2_213_Z98c`）已在此板**跑通，黑/白/红三平面正常**（音标红色验证）。两条关键经验：
> 1. **RESE 电阻**：B/W 屏用 **2.2Ω**；换三色屏需改为 **0.47Ω**——三色电荷泵电流需求更高，2.2Ω 限流太紧导致 BUSY 超时、屏不显示。换回 B/W 屏时记得改回 2.2Ω。
> 2. **烧录/进下载模式无需断开任何器件（2026-08 复核实）**：旧注记“需临时断开 GPIO16↔RST”作废——原理图重新核对（二极管位号为 D4，非 R11）+ 实测：RST 网络零直流负载，USB-TTL 直接驱动 RST 焊盘即可；烧录与定时深睡唤醒均正常。

## 7. 按键（低电平有效，按下到 GND）

| 按键 | 位号 | 连接 | 说明 |
|---|---|---|---|
| 上键 | SW2 | **IO0** ↔ GND（**串 R5 100Ω**） | 唯一功能键（下一个词）；strap 引脚，上电按住=下载模式 |
| 唤醒 | SW4 | RST ↔ GND（R33 10k 上拉） | 复位/唤醒（不占 GPIO） |

> 下键 SW1 已取消。

## 8. TF 卡（已移除）

> **TF 卡已去掉**（词典在 flash/PROGMEM，参考固件也不用它）。原占用的 IO12/IO5 释放：
> - IO5 → 改接显示屏 DC
> - IO12 → 纯电池采样使能
> 若日后要加回 TF：IO13/IO14 复用屏 SPI，CS 需另找引脚（IO5 已被 DC 占用）。

## 9. 烧录接口（外接 USB-TTL，无板上 CH340）

> 省去 CH340C 及其自动下载电路，改留 **6P 烧录焊盘/排针**，用外接 USB-TTL 刷固件。

| 焊盘 | 信号 | 说明 |
|---|---|---|
| 1 | TXD0 | ESP TX → 外部 USB-TTL RX |
| 2 | RXD0 | ESP RX ← 外部 USB-TTL TX |
| 3 | GPIO0 | 下载模式选择（烧录时拉低） |
| 4 | RST(EN) | 复位（烧录时触发进入下载模式） |
| 5 | GND | 地 |
| 6 | VCC_3V3 | 3.3V（可选，用于外部供电/参考） |

**烧录流程**（外接 USB-TTL）：
1. USB-TTL 接：`TX→RXD0`、`RX→TXD0`、`GND→GND`（3.3V 逻辑，勿接 5V TX）
2. **GPIO0 拉低** → 给 RST 一个低脉冲（或 EN 断电重启）
3. 进入下载模式，`esptool`/PlatformIO 烧录
4. 烧完**GPIO0 恢复高**、复位，正常启动

**保留**：USB-C 座（TYPEC16P）仅作**充电**（VBUS→D2→TP4054），D+/D- 悬空；R3/R6（5.1k CC 下拉）保留；**充电切换电路（Q8/R10/C19，见 §4）**保留。
**移除**：CH340C（CH1C）、USB 数据通路（R2/R15 470R）、自动下载电路（Q2/Q3/Q7 + R9/R16/R17）。

## 10. 电池电压检测（2026-08 改版：Q1 门控 + Q5 高边开关 + R15/R16 分压）

**2026-08 改版**：由原“单 N-MOS 低边开关 + R12(4.7k)/R7(1k)”（比 ×1/5.7、裸片 TOUT 0–1V）改为 **Q1(N-MOS) 门控 + Q5(P-MOS) 高边开关 + R15(100k)/R16(150k) 分压 + C20(10nf) 滤波**，ADC 按 **3.3V 基准**。原理图见仓库根 `电量检测.png`。

```
高边采样开关（Q5 SI2305 P-MOS）：
   BAT+ ── Q5.S │ Q5.D ── R15(100k) ── (ADC 结点) ── R16(150k) ── GND
                          │                      │
                          │ (C20 10nf) ── GND     ADC → ESP8266 A0
   Q5.G ── R14(100k) ── BAT+        （默认：Q5.G=BAT+ → V_GS=0 → Q5 关断）
   Q5.G ── Q1.D（漏极）                （Q1 导通时：Q5.G→GND → V_GS≈-V_BAT → Q5 导通）

门控（Q1 SI2302 N-MOS）：
   Q1.S ── GND
   Q1.G ── R21(100k) ── GND   （复位/未初始化时可靠拉低，Q1 关断）
   Q1.G ── R11(10k) ── IO12   （IO12=3.3V → Q1.G≈3.0V → Q1 导通）
```

| 状态 | IO12 | Q1(N-MOS) | Q5.G | Q5(P-MOS) | 采样回路 | 静态 |
|---|---|---|---|---|---|---|
| 不采样 | LOW/浮空 | 截止（R21 下拉） | =BAT+（R14 上拉） | 关断 | 断开 | ≈0 |
| 采样 | HIGH(3.3V) | 导通 | 拉至 GND | 导通(V_GS≈-V_BAT) | BAT+→R15→ADC→R16→GND | 分压电流 |

- **分压比**：`V_ADC = V_BAT × R16/(R15+R16) = ×150k/250k = 0.6`（忽略 Q5 的 R_DS(on)，毫欧级）；R14 **不参与分压**（仅 Q5 栅极门控）。
  - `V_BAT = V_ADC × 250/150 = ×5/3`；满电 4.2V → V_ADC≈2.52V
  - **ADC 按 3.3V 基准**（10bit 0–1023）：`V_BAT_mV = raw/1023 × 3300 × 250/150`；4.2V→raw≈780、3.5V→raw≈648、3.0V→raw≈557
- **⚠️ ADC 量程待实测确认**：上式按 3.3V 基准（设计公式）。若此板 ADC 实为裸片 TOUT 0–1V，则 4.2V 电池对应 2.52V 会**超量程/削顶**——接电池后用串口 `batt raw=` + 万用表对照定标，据实调整固件 `BATT_ADC_VREF`（3300 或 1000）与满量程。
- **元件功能**：IO12=采样使能（MCU GPIO）；R11(10k) 栅极串阻、R21(100k) Q1 栅极下拉；Q1 电平转换拉低 Q5 栅极；R14(100k) Q5 栅极上拉到 BAT+（默认关断）；Q5 高边电源开关；R15/R16 精密分压；C20(10nf) ADC 滤波 + SAR 电荷缓冲。
- **IO12**：TF 已移除，IO12 现为电池采样使能专用，无冲突。
- **采样时机**：每次刷新前 IO12 拉高 → 稳定 ~30ms（C20×R15 τ≈1ms，留裕量）→ `analogRead(A0)`×16 平均 → IO12 拉低。
- **移植**：参考固件 `readBatteryMv()`（ESP32 GPIO35 ×2 分压）不适用于本电路；固件已按 ×5/3、3.3V 基准实现（`src/word_cards.cpp` `readBatteryMv`）。

## 11. LED

| 位号 | 位置 | 作用 |
|---|---|---|
| LED0603_RED | TP4054 充电区 | 充电指示灯（接 CHRG 输出） |
| LED1 | 模组上方 | 状态灯（串 R29 驱动） |

## 12. 固件移植指引

`src/word_cards_ref.cpp` 是 **ESP32 参考实现**（行为参考），真正的固件应针对本原理图的 ESP8266（ESP-12S）编写。
`platformio.ini` 已配置 esp8266 环境，可直接在此基础上新建/改写固件。引脚映射如下：

| 功能 | 参考固件（ESP32） | 本硬件（ESP8266） |
|---|---|---|
| 显示屏 CS | GPIO5 | IO15 |
| 显示屏 DC | GPIO17（ESP8266 无此脚） | **IO5** |
| 显示屏 RST | GPIO16 | IO2 |
| 显示屏 BUSY | GPIO4 | IO4（一致） |
| 按键 | GPIO39（ESP8266 无此脚） | 上键 IO0 / 唤醒 RST |
| LED | GPIO19（ESP8266 无此脚） | LED1（R29） |
| 电池 ADC | GPIO35 | ADC(TOUT)，0–1V 量程 |
| 电池采样使能 | — | IO12（HIGH 接通分压） |
| 深睡唤醒 | — | GPIO16 |

参考固件用到的 ESP32 专属 API，在 ESP8266 上的替代方案：

| ESP32 API（参考固件） | ESP8266 替代 |
|---|---|
| `Preferences`（NVS） | EEPROM / LittleFS 文件 |
| `esp_light_sleep_start()` | `ESP.deepSleep(60s, WAKE_RF_DISABLED)`（唤醒已由 IO7 上拉硬件修复，见上文 BOOT 块注记）；FPM light sleep 不可用 |
| `esp_sleep_enable_ext0_wakeup(GPIO_NUM_39)` | SW4(RST) 手动刷（深睡中按 RST 复位） |
| `esp_random()` | `ESP.getRandom()` / `os_random()` |
| `analogReadMilliVolts()` | `analogRead(A0)`（0–1V，10bit） |

> ✅ **固件已落地（2026-08）**：`src/word_cards.cpp` 为 ESP8266 正式固件（**深睡版**：`ESP.deepSleep` 60s 自动刷词 + SW4/RST 手动刷；快刷为主每 10 次全刷清残影；计数经 RTC 内存 + LittleFS 断电保持）。关键要点：
> - **驱动类可切**：`USE_SSD1680`（1=SSD1680 `GxEPD2_213_B74`；0=IL3895 `GxEPD2_213`），两屏接线相同（IO15/IO5/IO2/IO4/IO13/IO14）。
> - **flash 直读必须 `pgm_read_byte`**：ESP8266 flash 映射区只支持 32 位访问，直接字节读 → LoadStoreError（曾致固件空白）。`src/u8g2_fonts_flash.h` 已把 u8g2 的 `u8x8_pgm_read` 重定义为 `pgm_read_byte`。
> - 中文字体单用 wqy14（~252KB，1MB 固件上限内）；快刷为主 + 每 10 次全刷清残影（`FULL_EVERY=10`）。
> - 手动刷词用 **SW4（RST）**，一次按下=一个词（IO7 上拉修复后单按即醒，不再需二次按压）。
